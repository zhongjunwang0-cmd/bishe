# Deep learning enabled by default (thesis / production).
# Set to 1 only for dev without T5 weights (rule fallback).
GRAMMAR_RULE_ONLY=0

# Path to trained artifacts (dkt_model.pt, grammar_t5/, etc.)
MODELS_DIR=../ml/models

# Preload Whisper on startup (pronunciation scoring)
PRELOAD_WHISPER=1
